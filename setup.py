from setuptools import setup, find_packages  # type: ignore
import sys, pathlib

setup(
    name="certifier",
    #    packages=["certifier"],
    #    data_files=("certifier/actions.pyi", "certifier/certifier.pyi"),
    #    package_data={
    #        "certifier": ["py.typed"],
    #    },
    #    zip_safe=False,
)
